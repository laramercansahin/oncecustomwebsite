# Secrets / env files — repo is public (GitHub Pages), never commit real credentials
.env
.env.*
*.pem
*.key
secrets.json

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node (in case any tooling is added later)
node_modules/
npm-debug.log*
